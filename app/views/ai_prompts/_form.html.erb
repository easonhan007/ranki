<%= form_with(model: ai_prompt) do |form| %>
  <% if ai_prompt.errors.any? %>
    <div style="color: red">
      <h5><%= pluralize(ai_prompt.errors.count, "error") %> prohibited this ai_prompt from being saved:</h5>

      <ul>
        <% ai_prompt.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-2">
    <%= form.label :name, class: 'form-label' %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="mb-2">
    <%= form.label :content, class: 'form-label' %>
    <%= form.text_area :content, class: 'form-control', rows: 20 %>
  </div>

  <div class="mb-2">
    <%= form.submit(class: 'btn btn-outline-primary') %>
  </div>
<% end %>
