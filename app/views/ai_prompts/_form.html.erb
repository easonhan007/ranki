<%= form_with(model: ai_prompt) do |form| %>
  <% if ai_prompt.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ai_prompt.errors.count, "error") %> prohibited this ai_prompt from being saved:</h2>

      <ul>
        <% ai_prompt.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
